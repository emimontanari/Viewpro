# Apply Progress: Seller Home Reference Fidelity

## I1A — seller adapters/extraction

- **Scope/base:** every phase row inherits `feat/seller-home-reference-fidelity-i1a` from exact base tree `develop@5ff7036d1129a08bddc783c1b2b2938f3dc556da`, the six-file allowlist, and final accounting; I1A only.
- **Environment:** 2026-09-08T10:54:51Z focused C exit **1** (`vitest: command not found`; missing dependencies) and linked `pnpm exec openspec` exit **243** (`spawn openspec EACCES`). Parent repaired stale cross-worktree workspace-package symlinks with target-worktree `pnpm install --offline --frozen-lockfile` and `pnpm --filter @viewpro/contracts build`, both exit **0**.
- **RED — 2026-09-08T10:58:47Z / 11:31:53Z / 12:03:43Z:** initial focused C exit **1** because `./seller-home` was missing; parity command `pnpm --filter next-shadcn-dashboard-starter test src/features/dashboard/components/operational-homepage.test.tsx -t 'keeps retained product refresh errors'` exit **1** because retained-error did not render `No se pudo cargar tu resumen…`; seller-home test exit **1**: `data.counters` null destructure TypeError.
- **GREEN — 2026-09-08T11:34:37Z / 12:05:32Z:** seller container/adapters, retained-error parity, and null-counter guard passed focused C (`pnpm --filter next-shadcn-dashboard-starter test src/features/dashboard/components/operational-homepage.test.tsx src/features/dashboard/components/operational-homepage/seller-home.test.tsx src/features/dashboard/components/operational-homepage/seller-sections.test.tsx`) exit **0**, **14 tests**.
- **TRIANGULATE — 2026-09-08T12:05:32Z:** pure matrix proves both sources' loading, ready-zero, refresh, retained-error, error/retrying plus null/missing/NaN counters and item-identity contrasts; BFF command `pnpm --filter next-shadcn-dashboard-starter test src/app/api/activity/feed/route.test.ts src/lib/bff-api.test.ts src/features/products/api/queries.test.ts src/features/products/api/service.test.ts` exit **0**, **13 tests**.
- **REFACTOR — 2026-09-08T12:05:32Z:** Protected command `pnpm --filter next-shadcn-dashboard-starter test src/features/dashboard/components/operational-homepage/manager-home.test.tsx src/features/owner/components/owner-home.test.tsx` exit **0**, **47 tests**; `pnpm --filter next-shadcn-dashboard-starter typecheck` and `pnpm --filter next-shadcn-dashboard-starter lint:strict` exit **0**; `npx --yes @fission-ai/openspec validate seller-home-reference-fidelity --strict` exit **0** (`Change 'seller-home-reference-fidelity' is valid`).
- **LSP / cleanup:** parent ran diagnostics on `operational-homepage.tsx`, `operational-homepage.test.tsx`, `seller-home.tsx`, and `seller-home.test.tsx`: **0 diagnostics**. Removed ignored target-worktree `apps/app-new/tsconfig.tsbuildinfo` after validation; `node_modules` is the target-worktree directory created by the offline frozen install.
- **Accounting/reason:** **385 changed lines** (`317+ / 68-` against base; 114-line source, 116-line test, 12-line evidence), within the 400-line budget; `git diff --check` exit **0**. I1A is complete; I1B remains deferred.

## I1B — preflight correction
- **Correction — 2026-09-08T12:37:14Z:** The prior blocked entry was incorrect. The required delta specification is present at `openspec/changes/seller-home-reference-fidelity/specs/crm-seller-home/spec.md` (272 lines). Parent-confirmed scoped status for this exact worktree/change is `next_action: begin`, with no delivery decision required. The injected null-change status referred to another worktree and is not applicable.
- **Scope/base/allowlist:** branch `feat/seller-home-reference-fidelity-i1b`; HEAD and local `origin/develop` `e85d3f16d877ae43e84cfd9185409142cfb76b6c`; I1B's four source/test files plus `tasks.md` and this evidence file only. Before any I1B edit, `git diff --check` exited **0** and the tree was clean.
## I1B — seller gates and truthful current composition
- **Shared context:** all phase commands ran from `viewpro-app/` on `feat/seller-home-reference-fidelity-i1b` at `e85d3f16d877ae43e84cfd9185409142cfb76b6c`, against local `origin/develop` at the same SHA; the I1B allowlist was enforced. Strict-TDD safety net C command exited **0** at 2026-09-08T09:39:54Z (**14 tests**).
- **RED — 2026-09-08T09:43:56Z:** C command `pnpm --filter next-shadcn-dashboard-starter test src/features/dashboard/components/operational-homepage.test.tsx src/features/dashboard/components/operational-homepage/seller-home.test.tsx src/features/dashboard/components/operational-homepage/seller-sections.test.tsx` exited **1**: new exact-role/membership mismatch assertion lacked neutral loading, and new local-unavailability/refresh assertions lacked named alerts/status. The initial real-QueryClient test parse defect was corrected before production code; it did not supply RED evidence.
- **GREEN — 2026-09-08T09:48:16Z:** the same C command exited **0** (**18 tests**) after exact seller identity and membership/tenant gates, independent owner state UI, and own-query retry wiring.
- **TRIANGULATE — 2026-09-08T09:49:17Z:** C proved product-error/activity-ready, activity-error/product-ready, retries, initial loading, and same-tenant refresh/retained-error; BFF command `pnpm --filter next-shadcn-dashboard-starter test src/app/api/activity/feed/route.test.ts src/lib/bff-api.test.ts src/features/products/api/queries.test.ts src/features/products/api/service.test.ts` exited **0** (**13 tests**).
- **REFACTOR — 2026-09-08T09:51:19Z / 09:53:32Z:** formatted I1B state helpers and test setup without behavior change; C exited **0** (**18 tests**), Protected command `pnpm --filter next-shadcn-dashboard-starter test src/features/dashboard/components/operational-homepage/manager-home.test.tsx src/features/owner/components/owner-home.test.tsx` exited **0** (**47 tests**), and typecheck/lint commands each exited **0**. `npx --yes @fission-ai/openspec validate seller-home-reference-fidelity --strict` exited **0** (`Change 'seller-home-reference-fidelity' is valid`). Parent LSP checked the three changed TS/TSX files with **0 diagnostics**; targeted lens scan found only planned/pre-existing presentation warnings.
- **Completion/accounting:** marked only the four I1B implementation rows `[x]`; no I2+ work started. `tsconfig.tsbuildinfo` was removed after checks. Final accounting is **316+ / 84- = 400**, within the I1B budget; `git diff --check` exited **0**. No design deviation or protected/API/BFF/manager/owner change.
### TDD Cycle Evidence
| Task | Test file | Safety net | RED | GREEN | TRIANGULATE | REFACTOR |
| --- | --- | --- | --- | --- | --- | --- |
| I1B | `operational-homepage.test.tsx`, `seller-home.test.tsx` | C 14/14 | Named gates/state assertions failed | C 18/18 | C + BFF 13/13 | C + Protected 47/47 + typecheck/lint |
## I1B verifier-driven proof correction
- **Correction — 2026-09-08T10:02:43Z / 10:45:17Z:** Removed the manual-cache-key `QueryClient` test because it did not mount production query options, seller container, or membership/tenant remount. I1B now proves only role/gates and visible independent owner states: confirmed activity row during product failure, confirmed assigned property during activity failure, product and activity refresh/retained contrasts, and disabled retrying state with owner-only retry callbacks. RED C exited **1** on activity freshness cardinality; GREEN C exited **0** (**16 tests**) after adding adjacent activity-summary/priority `Actualizando…`/`Última información disponible` while ready remains value-only; BFF **13**, Protected **47**, typecheck/lint, and strict OpenSpec passed. I1C is pending for the real `SellerOperationalHomepage` production-query A→B transition and same-tenant retained proof; no I1B claim represents that pending evidence. Parent LSP 0 diagnostics remain accurate.

## I1C — Production tenant-transition proof

- **Scope/base/status:** `test/seller-home-reference-fidelity-i1c` at fresh `develop@2d0db0529d74aedc4dbcdd724786cac89b8a865b` after I1B #569; UTC **2026-09-08T14:18:39Z**. The parent supplied scoped runtime status `next_action: begin` with no delivery decision. The injected null-change/missing-artifact status belongs to another cwd and was ignored as instructed. Action context is repo-local with the target worktree as the sole allowed root; no warnings. Only `seller-home.test.tsx`, `tasks.md`, and this file changed; no production source, authorization, cache-key invention, visual redesign, or I2+ work.
- **Safety net:** from `viewpro-app/`, `pnpm --filter next-shadcn-dashboard-starter test src/features/dashboard/components/operational-homepage/seller-home.test.tsx` exited **0** with **3 tests** before I1C edits.
- **RED / GREEN-on-arrival probe:** added the real `SellerOperationalHomepage` + `QueryClientProvider` tenant-A→tenant-B keyed membership probe before any production edit, mocking only `getProducts` and `getActivityFeed` transports. Its principal A-late/B-loading/B-ready assertion passed on arrival, demonstrating I1A/I1B's existing tenant query ownership/remount behavior; no artificial RED or production break was made. The first six-test execution exited **1** only because later newly-added variants inherited mock call history (two test-harness assertions), while the new production race assertion itself passed. After isolated `beforeEach(vi.resetAllMocks)` hygiene, the focused command exited **0** with **6 tests**.
- **GREEN:** the real container renders state tenant IDs, fixture totals/counters, and fixture row IDs; after switching `membership-a:tenant-a` to `membership-b:tenant-b`, late A results never appear beneath B, B stays B-owned loading, and then renders B-owned ready data. The same-tenant case invokes `queryClient.refetchQueries` with the real `productsQueryOptions(...).queryKey`, proves `refreshing` with A's retained value/row, rejects that refetch, and proves `retained-error` with the same retained value/row. No production implementation change was required.
- **TRIANGULATE:** the error variant holds B-owned loading then B-local error while A resolves late; it asserts no A rows remain visible. The ready variant asserts actual `productsQueryOptions` and `activityFeedOptions` keys each include `tenantId`, their mounted query options retain `refetchOnReconnect: false` and `refetchOnWindowFocus: false`, and products/activity keys differ. Focused C command `pnpm --filter next-shadcn-dashboard-starter test src/features/dashboard/components/operational-homepage.test.tsx src/features/dashboard/components/operational-homepage/seller-home.test.tsx src/features/dashboard/components/operational-homepage/seller-sections.test.tsx` exited **0** with **19 tests**.
- **REFACTOR/verification:** retained only the production-container proof and transport mocks. BFF command `pnpm --filter next-shadcn-dashboard-starter test src/app/api/activity/feed/route.test.ts src/lib/bff-api.test.ts src/features/products/api/queries.test.ts src/features/products/api/service.test.ts` exited **0** with **13 tests**; Protected command `pnpm --filter next-shadcn-dashboard-starter test src/features/dashboard/components/operational-homepage/manager-home.test.tsx src/features/owner/components/owner-home.test.tsx` exited **0** with **47 tests**. `pnpm --filter next-shadcn-dashboard-starter typecheck` and `pnpm --filter next-shadcn-dashboard-starter lint:strict` each exited **0**. Parent LSP checked the changed `seller-home.test.tsx`: **0 diagnostics**; targeted lens scan found no issue.
- **Tasks/accounting:** marked exactly the four I1C implementation rows `[x]` and preserved all parent-owned rows byte-for-byte. `npx --yes @fission-ai/openspec validate seller-home-reference-fidelity --strict` exited **0** (`Change 'seller-home-reference-fidelity' is valid`); ignored `viewpro-app/apps/app-new/tsconfig.tsbuildinfo` was removed; `git diff --check` exited **0**; `git diff --stat` and `git status --short` show only the three allowed files. Additions/deletions/total: **225 / 24 / 249**, within the 400-line I1C budget. No design deviation. Remaining implementation rows are I2–I4; deferred lifecycle actions are all parent-owned task rows.

### TDD Cycle Evidence
| Task | Test file | Layer | Safety net | RED | GREEN | TRIANGULATE | REFACTOR |
| --- | --- | --- | --- | --- | --- | --- | --- |
| I1C | `seller-home.test.tsx` | Integration (`QueryClientProvider` + real container/query options; transport services mocked) | 3/3 | GREEN-on-arrival production race probe; initial 6-test run exposed only mock-history test hygiene | 6/6 after test-only isolation | A-late/B-ready, B-loading/error, and same-tenant refetch rejection | C 19/19 + BFF 13/13 + Protected 47/47 + typecheck/lint |

## I1C — reviewer P1 corrective proof

- **Scope/status/base:** reviewer-directed P1 correction only on `test/seller-home-reference-fidelity-i1c`, cumulative base `develop@2d0db0529d74aedc4dbcdd724786cac89b8a865b`; UTC **2026-09-08T14:40:22Z**. Scoped user instruction selects this existing change and correction after PR #571 review; it is repo-local within the target worktree, with no edit-root warnings. The prior I1C task was all checked, but this corrective proof remediates its inaccurate RED/remount evidence; no I2+ work or protected-surface behavior changed.
- **Safety net / initial probe:** `pnpm --filter next-shadcn-dashboard-starter test src/features/dashboard/components/operational-homepage/seller-home.test.tsx` exited **0** with **6 tests** before correction. The prior tenant A→B response-isolation assertion remains an honest GREEN-on-arrival probe.
- **Corrective RED:** removed the test-owned external React key, passed `membershipId` through the public component API, and placed a stateful `MountedTenantProbe` in the real container child. Before wrapper wiring, `pnpm --filter next-shadcn-dashboard-starter test src/features/dashboard/components/operational-homepage/seller-home.test.tsx -t 'keeps late tenant-A responses'` exited **1** on the named assertion: expected `mounted:tenant-b`, received `mounted:tenant-a`. This proves the existing query-key isolation alone did not remount the mounted child.
- **Corrective GREEN:** `SellerOperationalHomepage` now owns `${membershipId}:${activeTenantId}` by rendering an internal keyed query container; `OperationalHomepage` supplies `membershipId` with no external key. The same named test exited **0** (**1 passed, 5 skipped**) and the full seller-home focused command exited **0** with **6 tests**. The test helper has no explicit React key and mocks only product/activity transport services.
- **TRIANGULATE / REFACTOR:** retained the A-late/B-loading/B-ready and B-error variants, real options/refetch-key proof, and same-tenant refresh/rejected-refetch proof; no further behavior refactor was needed. C command `pnpm --filter next-shadcn-dashboard-starter test src/features/dashboard/components/operational-homepage.test.tsx src/features/dashboard/components/operational-homepage/seller-home.test.tsx src/features/dashboard/components/operational-homepage/seller-sections.test.tsx` exited **0** (**19 tests**); BFF exited **0** (**13**); Protected manager/owner exited **0** (**47**); `pnpm --filter next-shadcn-dashboard-starter typecheck`, `pnpm --filter next-shadcn-dashboard-starter lint:strict`, `npx --yes @fission-ai/openspec validate seller-home-reference-fidelity --strict`, and `git diff --check` each exited **0**. Parent LSP checked the three changed TS/TSX files with **0 diagnostics**. Removed ignored `viewpro-app/apps/app-new/tsconfig.tsbuildinfo`; final `git status --short` lists only the five allowlisted files. The I1C RED task text now truthfully reads `PROBE/GREEN-on-arrival + corrective RED`; all four I1C implementation rows remain visibly checked. Cumulative additions/deletions/total against `2d0db052`: **262 / 30 / 292**, within the 400-line budget.

### TDD Cycle Evidence — reviewer correction
| Task | Test file | Layer | Safety net | PROBE | Corrective RED | GREEN | TRIANGULATE/REFACTOR |
| --- | --- | --- | --- | --- | --- | --- | --- |
| I1C P1 | `seller-home.test.tsx` | Integration | 6/6 | Original response race was GREEN-on-arrival | Mounted probe B assertion failed with A retained | Named 1/1; full seller test 6/6 | Existing ready/error/refetch variants plus C 19/19, BFF 13/13, Protected 47/47, typecheck/lint/OpenSpec |

## I2 — Reference summary composition

- **Scope/status/base:** UTC **2026-09-08T15:26:14Z**; `feat/seller-home-reference-fidelity-i2` at fresh `develop@9107ea1b` after I1C #571; scoped `next_action: begin` applies to this worktree. Repo-local action context permits only this worktree; the injected null-change status was for another cwd. Stacked-to-main I2 is the assigned ≤400-line PR boundary.
- **RED:** added `seller-sections.test.tsx`; focused C exited **1** because `./seller-sections` did not exist.
- **GREEN:** added pure `SellerHomeView` and cut over only the legacy seller hero/focus/KPI summary. C exited **0** (**22 tests**): real greeting/tenant, four owner-bound facts, rolling-window helper, exact two non-checkable priorities, and no zero substitution.
- **TRIANGULATE:** direct presentation cases cover activity unavailable with products retained, successful activity zeros while refreshing, and forbidden `hoy`/checkbox content; existing operational coverage keeps missing identity fail-closed and retained owner states. BFF exited **0** (**13**); protected manager/owner exited **0** (**47**).
- **REFACTOR/verification:** C **22/22**, full frontend test **802/802**, typecheck **0**, lint **0**, and `npx --yes @fission-ai/openspec validate seller-home-reference-fidelity --strict` **0**. Parent LSP remains parent-owned; `tsconfig.tsbuildinfo` was removed. `git diff --check` is clean.
- **Files:** `operational-homepage.tsx`, its narrow regression test, `seller-sections.tsx`, `seller-sections.test.tsx`, `tasks.md`, and this progress file. No design deviation, shell, BFF/API, manager, owner, I3 lists/shortcuts, or I4 browser work.
### TDD Cycle Evidence
| Task | Test file | Layer | Safety net | RED | GREEN | TRIANGULATE | REFACTOR |
| --- | --- | --- | --- | --- | --- | --- | --- |
| I2 | `seller-sections.test.tsx` | Component | C 19/19 | Missing module failed | C 22/22 | unavailable/zero/refresh + BFF 13/13 + Protected 47/47 | C/typecheck/lint/OpenSpec green |

- **Commands (all exit 0 unless RED):** Frontend `pnpm --filter next-shadcn-dashboard-starter test`; C `pnpm --filter next-shadcn-dashboard-starter test src/features/dashboard/components/operational-homepage.test.tsx src/features/dashboard/components/operational-homepage/seller-home.test.tsx src/features/dashboard/components/operational-homepage/seller-sections.test.tsx`; BFF `pnpm --filter next-shadcn-dashboard-starter test src/app/api/activity/feed/route.test.ts src/lib/bff-api.test.ts src/features/products/api/queries.test.ts src/features/products/api/service.test.ts`; Protected `pnpm --filter next-shadcn-dashboard-starter test src/features/dashboard/components/operational-homepage/manager-home.test.tsx src/features/owner/components/owner-home.test.tsx`; typecheck `pnpm --filter next-shadcn-dashboard-starter typecheck`; lint `pnpm --filter next-shadcn-dashboard-starter lint:strict`; OpenSpec `npx --yes @fission-ai/openspec validate seller-home-reference-fidelity --strict`.

## I2 — verifier corrective proof

- **UTC 2026-09-08T15:40:02Z / RED:** after the initial missing-module setup failure, focused `seller-sections.test.tsx` failed: five per-priority `Actualizando…`/`Última información disponible` labels were expected and four rendered.
- **GREEN/TRIANGULATE/REFACTOR:** each priority owns its `StateLabel`; fact/priority order, all forbidden summary groups, both unavailable-owner/sibling-success directions, zeros, refresh, and retained-error are asserted. C **23**, BFF **13**, Protected **47**, frontend **803**, typecheck, lint, strict OpenSpec, and `git diff --check` passed; `tsconfig.tsbuildinfo` was removed.
- **Boundary/LSP:** no design deviation or I3/I4 behavior; the I2 allowlist now names the narrow operational regression test plus tasks/progress. Parent LSP checked all five changed TS/TSX files, including the seeded scenario, with **0 primary diagnostics**; auxiliary findings were Spanish-copy typos only.

### TDD Cycle Evidence — verifier correction
| Task | Test file | RED | GREEN | TRIANGULATE / REFACTOR |
| --- | --- | --- | --- | --- |
| I2 correction | `seller-sections.test.tsx` | 5 freshness labels failed; 4 rendered | targeted 4/4 | C 23/23, BFF 13/13, Protected 47/47, frontend 803/803, typecheck/lint/OpenSpec |

- **Seeded correction:** CI job `102141409204` RED on the three stale selectors; only `tests/seeded/demo-smoke.spec.ts` now asserts `^Hola, `, exact main `DEMO_TENANT_NAME`, and `Abrir listado` → `/dashboard/product`. Local Docker `viewpro-postgres` only: `docker compose up -d postgres` **0**; `DATABASE_URL=postgresql://viewpro:viewpro@127.0.0.1:5432/viewpro pnpm --filter @viewpro/api db:generate` **0**; `DATABASE_URL=postgresql://viewpro:viewpro@127.0.0.1:5432/viewpro DIRECT_URL=postgresql://viewpro:viewpro@127.0.0.1:5432/viewpro pnpm --filter @viewpro/api db:validate` and `DATABASE_URL=postgresql://viewpro:viewpro@127.0.0.1:5432/viewpro DIRECT_URL=postgresql://viewpro:viewpro@127.0.0.1:5432/viewpro pnpm --filter @viewpro/api exec prisma migrate deploy` **0** (32 migrations, none pending). After avoiding a reused owner-preview server on 3001/3100, targeted `DATABASE_URL=postgresql://viewpro:viewpro@127.0.0.1:5432/viewpro DIRECT_URL=postgresql://viewpro:viewpro@127.0.0.1:5432/viewpro VIEWPRO_APP_NEW_SEEDED_E2E_API_PORT=3012 VIEWPRO_APP_NEW_SEEDED_E2E_WEB_PORT=3111 pnpm --filter next-shadcn-dashboard-starter exec playwright test --config playwright.seeded.config.ts --grep 'distinct assigned seller dashboard'` **0** (**2 passed**); full `DATABASE_URL=postgresql://viewpro:viewpro@127.0.0.1:5432/viewpro DIRECT_URL=postgresql://viewpro:viewpro@127.0.0.1:5432/viewpro VIEWPRO_APP_NEW_SEEDED_E2E_API_PORT=3013 VIEWPRO_APP_NEW_SEEDED_E2E_WEB_PORT=3112 pnpm --filter next-shadcn-dashboard-starter test:seeded` **0** (**34 passed**). Artifacts cleaned; tasks/progress scope updated; cumulative accounting is **400**.

## I3 — bounded content/cutover (blocked: review budget)

- **Scope/status:** repo-local target worktree `seller-home-reference-fidelity-i3` at `develop@a91f5f6f`; the supplied target-specific change selection supersedes the injected null-change status for another worktree. I3 is the assigned stacked-to-main slice, with no size exception.
- **Environment/safety net:** `pnpm install --offline --frozen-lockfile` and `pnpm --filter @viewpro/contracts build` exited **0** in `viewpro-app/`; focused C baseline exited **0** (**23 tests**).
- **RED:** new seller list/presenter assertions imported the absent `./seller-lists`; focused `seller-sections.test.tsx` exited **1** with the named module-resolution error before production code.
- **Budget stop:** the GREEN/triangulation candidate (bounded source-order lists, safe tenant/ID links, time formatting, empty wording, shortcut cutover, and tests) measured **251 additions / 157 deletions = 408 changed lines** before required task/progress evidence. Per the I3 ≤400 contract, all candidate source/test changes were reverted; only this evidence remains and every I3 checkbox remains unchecked.
- **Split proposal:** deliver seller-list presenters plus their direct tests first, then a successor cutover that moves the two list sections and nav-derived shortcuts out of `operational-homepage.tsx`; both retain the same I3 contract and must be measured independently.

- **TDD stop evidence:** C **23/23** safety net; `seller-sections.test.tsx` failed on missing `seller-lists`; no GREEN candidate was retained because it exceeded the budget.

- **Accepted split:** Human selected `Dividir I3`; deliver I3A then I3B as fresh-`develop`, ≤400-line sequential PRs with no `size:exception`.

## I3A — seller bounded list presenters

- **Scope/status/base:** 2026-09-08T17:50:05Z; exact user-selected change in the repo-local target worktree, action context limited to that root with no warnings. The parent’s accepted I3A split resolves the workload gate; I3B composition/shortcuts remain untouched.
- **RED:** direct `seller-sections.test.tsx` presenter import failed with the named missing `./seller-lists` module; the pre-edit safety net was **4/4**.
- **GREEN/TRIANGULATE:** added only `seller-lists.tsx` and direct tests. They prove six-or-fewer source-order rows, safe same-tenant links, neutral fields and positive-total-empty copy, movement/document stored prose, valid Argentina `<time>`, malformed neutral time, and successful-empty/error separation. C **26**, BFF **13**, and Protected **47** passed; verifier corrections were GREEN-on-arrival for exact long-prose preservation, blank observation/document fallbacks, blank/malformed IDs and times, normalized offset ISO, and the full forbidden matrix on both populated presenters. Its initial typecheck found only a fixture-array annotation mismatch, corrected without presenter behavior change.
- **REFACTOR/verification:** retained independent presenters with extracted field/link helpers; no `SellerHomeView`, `OperationalHomepage`, shortcut, manager, or shared-list change. Full frontend **806**, typecheck, strict lint, strict OpenSpec, and `git diff --check` passed; `tsconfig.tsbuildinfo` was removed. Parent’s prior LSP zero applies only to pre-correction files; rerun is parent-owned. Final correction accounting is **381 additions / 19 deletions = 400**.

### TDD Cycle Evidence
| Task | Test file | Layer | Safety net | RED | GREEN | TRIANGULATE | REFACTOR |
| --- | --- | --- | --- | --- | --- | --- | --- |
| I3A | `seller-sections.test.tsx` | Component | 4/4 | Missing presenter module | Direct 7/7 | C 26/26 + BFF 13/13 + Protected 47/47 | Full frontend 806 + typecheck/lint |

## I3B — seller composition cutover and shortcuts

- **Scope/base/status:** 2026-09-08T18:34:55Z; fresh `develop@8eff68f64008` after I3A #575. The user-selected repo-local change supersedes the null-change status from another cwd; allowed root is this worktree with no action-context warnings. I3B only; parent LSP checked all four changed TS/TSX files with **0 diagnostics**.
- **RED:** focused `seller-sections.test.tsx` exited **1** with the named missing composition assertion: expected the seller list and shortcut headings after priorities, but only the two summary headings rendered. A corrective retained-data RED then failed on the missing product-only retry while confirmed product/activity rows remained visible.
- **GREEN/TRIANGULATE:** `SellerHomeView` now owns the I3A property/activity presenters and final property→follow-up navigation. `OperationalHomepage` removes only seller inline lists/JSX and maps those two routes, labels, and icons from filtered existing `navGroups` after the exact `AGENT` gate. Retained presenters now keep confirmed rows and expose only their own retry. C **29**, BFF **13**, and Protected manager/owner **47** passed; contrasts cover independent failure/retry/refresh/retained data, success empty/positive-total empty, valid/invalid row links, and manager/unknown/mismatch isolation.
- **REFACTOR/verification:** full frontend **809**, typecheck, strict lint, strict OpenSpec, and `git diff --check` passed. No BFF/API/nav-config/access-policy/session/manager/owner/shared-list change and no I4 browser/geometry work. Only the four I3B implementation rows were checked; I4 and all parent rows remain unchecked.
- **Workload/boundary:** **187 additions + 126 deletions = 313** including evidence; I3B stays below 400. Changed files are the allowed seller composition/tests plus tasks/progress; no design deviation.

### TDD Cycle Evidence
| Task | Test file | Layer | Safety net | RED | GREEN | TRIANGULATE | REFACTOR |
| --- | --- | --- | --- | --- | --- | --- | --- |
| I3B | `seller-sections.test.tsx`, `operational-homepage.test.tsx` | Component/integration | C 26/26; Protected 47/47 | Composition heading and retained local-retry assertions failed | seller-sections 9/9 | C 29/29 + BFF 13/13 + Protected 47/47 | Frontend 809/809 + typecheck/lint/OpenSpec/diff |

## I3B — verifier accessibility correction

- **Scope/status:** 2026-09-08T18:43:56Z; user-directed I3B-only correction within the same repo-local root. The original null-change status remains inapplicable to this explicitly selected target; no action-context warning. No task checkbox changed because I3B is already complete and this correction uses the allowed source/test/progress surfaces only.
- **RED/GREEN/TRIANGULATE:** safety net `seller-sections.test.tsx` was **9/9**. New composed-region assertions then failed because whitespace-derived `aria-labelledby` values left both list sections unnamed. Replaced the runtime heading derivation with explicit `seller-engagements-heading` and `seller-activity-heading`; the headings retain their visible copy while the regions resolve as `Mis propiedades` and `Actividad reciente` and contain their respective valid row links. Focused component test returned **9/9**.
- **REFACTOR/verification:** focused C **29**, BFF **13**, Protected **47**, full frontend **809**, typecheck, strict lint, and `git diff --check` passed. No I4/browser/geometry work, route, policy, manager, owner, BFF, or API change. Parent LSP remains the final parent-owned check.

### TDD Cycle Evidence — verifier correction
| Task | Test file | Layer | Safety net | RED | GREEN / TRIANGULATE | REFACTOR |
| --- | --- | --- | --- | --- | --- | --- |
| I3B region names | `seller-sections.test.tsx` | Component | 9/9 | Named regions were absent | 9/9 with both stable IDs and independently named regions | C 29/29 + BFF 13/13 + Protected 47/47 + frontend/typecheck/lint |
