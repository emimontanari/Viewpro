# =================================================================
# viewpro-web — Operator Console Configuration
# =================================================================
# Copy this file to .env.local for local development.
# Never commit actual .env files to version control.

# Public backend API base URL consumed by api-client.ts (browser-facing).
# Points at viewpro-api (port 3002 locally).
NEXT_PUBLIC_API_URL=http://localhost:3002/api

# Dev port — set via the dev script (next dev -p 3003); override here if needed.
PORT=3003

# =================================================================
# Error Tracking (Sentry) — off by default
# =================================================================
# NEXT_PUBLIC_SENTRY_DSN=
# NEXT_PUBLIC_SENTRY_ORG=
# NEXT_PUBLIC_SENTRY_PROJECT=
# SENTRY_AUTH_TOKEN=
# NEXT_PUBLIC_SENTRY_ENABLED="false"

# =================================================================
# NOT set here:
# =================================================================
# ACCESS_TOKEN_SECRET — NOT set in the FE (D3: presence-check middleware
#   only; no JWT signature verification in viewpro-web). Authority is
#   viewpro-api AuthGuard + GET /auth/me.
# BFF_API_URL — no BFF in viewpro-web (Design B direct-to-api pattern).

# =================================================================
# CORS / cookie config note (add to viewpro-api .env):
# =================================================================
# LOCAL:
#   CORS_ORIGIN=http://localhost:3003
# PROD (cross-subdomain):
#   CORS_ORIGIN=https://console.viewpro.app
#   COOKIE_DOMAIN=.viewpro.app
#   COOKIE_SECURE=true
