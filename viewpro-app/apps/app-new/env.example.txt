# =================================================================
# ViewPro API Configuration
# =================================================================

# Server-only backend API base URL used by Next route handlers/BFF routes.
# Do not prefix this with NEXT_PUBLIC_; it may point to an internal API origin.
BFF_API_URL=http://localhost:3001/api

# Public backend API base URL used by browser-facing auth/proxy code.
# Keep the API configured to allow this app origin and credentials.
NEXT_PUBLIC_API_URL=http://localhost:3001/api

# Public app origin used by server-rendered client components when calling
# same-origin Next route handlers during local SSR.
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Server-only secret used by Next proxy to validate dashboard access tokens.
# Must match the API ACCESS_TOKEN_SECRET. Never prefix this with NEXT_PUBLIC_.
ACCESS_TOKEN_SECRET=change-me-in-real-env

# Optional demo latency for local mock route handlers.
# Keep false while developing dashboard navigation.
NEXT_PUBLIC_MOCK_API_DELAY="false"


# =================================================================
# Build Configuration
# =================================================================
# Set to 'true' when deploying with Docker or self-hosting on a VPS.
# This enables Next.js standalone output mode for smaller deployments.

BUILD_STANDALONE=    # Example: "true"


# =================================================================
# Error Tracking Configuration (Sentry)
# =================================================================
# To set up Sentry error tracking:
# 1. Create an account at https://sentry.io
# 2. Create a new project for Next.js

NEXT_PUBLIC_SENTRY_DSN=    # Example: https://****@****.ingest.sentry.io/****
NEXT_PUBLIC_SENTRY_ORG=    # Example: acme-corp
NEXT_PUBLIC_SENTRY_PROJECT=    # Example: nextjs-dashboard
SENTRY_AUTH_TOKEN=    # Example: sntrys_************************************
NEXT_PUBLIC_SENTRY_DISABLED="false"
NEXT_PUBLIC_SENTRY_ENABLED="false" # Set to "true" only when testing Sentry locally


# =================================================================
# Important Notes:
# =================================================================
# 1. Rename this file to '.env' for local development
# 2. Never commit the actual '.env' file to version control
# 3. Make sure to replace all placeholder values with real ones
# 4. Keep secret values private and never share them
